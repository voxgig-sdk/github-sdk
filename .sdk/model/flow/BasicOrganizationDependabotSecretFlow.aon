# BasicOrganizationDependabotSecretFlow

main: kit: flow: BasicOrganizationDependabotSecretFlow:
{
  entity: "organization_dependabot_secret",
  kind: "basic",
  name: "BasicOrganizationDependabotSecretFlow",
  step: [
    {
      input: {
        ref: "organization_dependabot_secret_ref01",
        srcdatavar: "organization_dependabot_secret_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "organization_dependabot_secret01",
        org_id: "org01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-organization_dependabot_secret_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicOrganizationDependabotSecretFlow"
}