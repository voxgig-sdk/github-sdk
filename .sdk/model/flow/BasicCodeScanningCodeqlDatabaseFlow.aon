# BasicCodeScanningCodeqlDatabaseFlow

main: kit: flow: BasicCodeScanningCodeqlDatabaseFlow:
{
  entity: "code_scanning_codeql_database",
  kind: "basic",
  name: "BasicCodeScanningCodeqlDatabaseFlow",
  step: [
    {
      match: {
        owner: "owner01",
        repo: "repo01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "code_scanning_codeql_database_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "code_scanning_codeql_database_ref01",
        srcdatavar: "code_scanning_codeql_database_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "code_scanning_codeql_database01",
        owner: "owner01",
        repo: "repo01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-code_scanning_codeql_database_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicCodeScanningCodeqlDatabaseFlow"
}