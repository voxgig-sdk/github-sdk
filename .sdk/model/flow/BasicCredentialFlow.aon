# BasicCredentialFlow

main: kit: flow: BasicCredentialFlow:
{
  entity: "credential",
  kind: "basic",
  name: "BasicCredentialFlow",
  step: [
    {
      input: {
        ref: "credential_ref01"
      }

      op: "create"
    }
  ],
  "key$": "BasicCredentialFlow"
}